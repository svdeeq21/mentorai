--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
torchvision==0.17.2+cpu
# MentorAI Backend Requirements - Production (Railway)
# Excludes torch/CUDA/unstructured-inference to keep build under timeout

# ── Web framework ─────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# ── Rate limiting ─────────────────────────────────────────
slowapi==0.1.9

# ── Auth & Database ───────────────────────────────────────
supabase==2.7.4
python-dotenv==1.0.1

# ── LLMs ─────────────────────────────────────────────────
langchain-google-genai==2.0.1
langchain-groq==0.2.0
langchain-core==0.3.15
google-generativeai==0.8.3
groq==0.11.0

# ── Vector DB ─────────────────────────────────────────────
qdrant-client==1.12.1

# ── Embeddings (CPU-only, no torch/CUDA) ──────────────────
langchain-huggingface==0.1.0
sentence-transformers==3.2.1
torch==2.2.2

# ── Document processing (no unstructured-inference) ───────
unstructured[pdf]>=0.17.2
pdf2image==1.17.0
pillow==11.0.0
pymupdf==1.24.14
pytesseract==0.3.13

# ── HTTP client ───────────────────────────────────────────
httpx==0.27.2

# ── Pydantic ─────────────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.5.2
